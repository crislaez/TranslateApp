import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.traductorApp',
  appName: 'traductorApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
